# meat-riyadh-app

# شرح المستوى 1: نظام حالات الطلب

## ماذا سنفعل؟

### 1. إضافة حقل "الحالة" (Status) للطلب
- عند إنشاء طلب جديد → الحالة = "معلق" (Pending)
- المشتري يرى حالة طلبه محدّثة في "طلباتي"
- يمكن تغيير الحالة يدويًا من واجهة الطلب

### 2. نظام الألوان والرموز
```
⏳ معلق (Pending) → لون أصفر/برتقالي
⚙️ قيد التحضير (Processing) → لون أزرق/أخضر فاتح
🚚 تم التوصيل (Delivered) → لون أخضر غامق
❌ ملغى (Cancelled) → لون أحمر
```

### 3. التخزين المحلي (localStorage)
كل طلب سيحتوي على:
```javascript
{
  id: 1764160464949,
  status: "معلق",  // ← الحقل الجديد
  date: "...",
  animal: "سواكني",
  // ... باقي البيانات
}
```

## خطوات التنفيذ:

### الخطوة 1: إضافة CSS للحالات (Status Badge)
نضيف أنماط لتظهر الحالة بألوان مختلفة

### الخطوة 2: تعديل دالة حفظ الطلب
عند استقبال طلب جديد → status = "معلق"

### الخطوة 3: عرض الحالة في قائمة الطلبات
إضافة badge ملون يظهر بجانب رقم الطلب

### الخطوة 4: أزرار تغيير الحالة
أزرار صغيرة لتحديث حالة الطلب:
- [ معلق ] [ قيد التحضير ] [ تم التوصيل ]

## الفوائد التعليمية:
✓ فهم localStorage بشكل أعمق
✓ التعامل مع تحديث البيانات ديناميكياً
✓ تحسين واجهة المستخدم
✓ تمرين على JavaScript متوسط المستوى
